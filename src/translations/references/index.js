import { defineMessages } from 'react-intl';

const translations = defineMessages({
  noReference: {
    id: 'section.references.noReference',
    defaultMessage: 'References available on request'
  }
});

export default translations;