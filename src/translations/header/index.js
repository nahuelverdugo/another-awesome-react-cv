import { defineMessages } from 'react-intl';

const translations = defineMessages({
  imageAlt: {
    id: 'section.header.imageAlt',
    defaultMessage: 'CV photo'
  }
});

export default translations;